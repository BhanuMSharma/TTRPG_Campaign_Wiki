GENERAL
* you should be able to upload photos for wiki pages
* Enhance the content display in both Wiki Pages and Adventure Logs - 
* Add better navigation between pages
* sidebar campaign home button remove, and just make that title the button

CAMPAIGN LIST
* or is declared but not used
* delete button on private campaign page - but only for if created by user

CAMPAIGN HOME
* campaign home page should have another card above wiki pages card and adventure logs card (these should be way smaller, btw) that is Most Recent Adventure Log
* user const is declared but not used

WIKI PAGE LIST
* There should be a sort and filter option

ADVENTURE LOGS
* adventure logs page should always be in correct order
* adventure logs dates are not updating or saving
** also when selecting the date, by default it should be on current date (not 01/01/2000)


* VISUAL FIXES
** Improve the visual design and styling of the entire application
** you can also upload a photo for the campaigns (this is displayed as background to campaign cards in campaignList and as banner photo at top of campaignHome)
** wiki pages list - the cards should be smaller
** campaign home font and colors


CHANGES DONE:
v2
wikipageview created date fixed
campaignlist card dates fixed and formatDate function removed
adventureloglist log card styling const removed
wikipagelist wiki card styling const removed
campaignUrls, wiki page URLs are now easier to read

v3
adventurelog url changed (index.ts, AdvenutreLogCreate.tsx, AdventureLogList.tsx, AdventureLogView.tsx, CampaignHome.tsx)

v4
added global.css, and theme folder (global.css, /theme/index.ts)
logo changed and site title changed on browser tab (index.html)
sidebar is collapsible on mobile devices or windows smaller than ac ertain size (width: 768px) (sidebar.tsx and app.tsx)
campaign home button in sidebar removed and the title of the campaign is now the home button (sidebar.tsx)
navbar title changed from d&d campaign wiki to ttrpg campaign wiki (Navbar.tsx)
added campaign type badge to campaigns (campaigncreate.tsx, campaignhome.tsx, campaignlist.tsx, types/index.ts)
fixed issue where sidebar wasnt showing campaign name by calling campaign info differently (sidebar.tsx)
campaign list page changed from d&d campaigns to TTRPG campaigns (campaignlist.tsx)

v5
bug fixes related to previous update, specifically theme (app.tsx, sidebar.tsx, theme/index.ts)
